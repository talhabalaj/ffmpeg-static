# FFmpeg (libshine only)
This static binary is built to work on `heroku-18` stack and is used for a buildpack. 

# Why? 
I only needed a faster Mp3 encoder for my (WhatsApp bot)[https://github.com/talhabalaj/wayt-bot].

